data Bool : Type;
data Int : Type;
data Float : Type;

data MyType
    constructor1 : Int → MyType
    constructor2 : Float → MyType
;

one : Int = _
pi : Float = _
true : Bool = _
abs : Int → Int = _
add : Int → Int → Int = _
id : ∀a ⇒ a → a = _
float_to_int : Float → Int = _
polymorphic : ∀a ⇒ a = _
scoped : ∀a ⇒ (∀s ⇒ s → a) → a = _
f = one

data Bool : Type;
data Int : Type;
data Float : Type
;

data MyType
    constructor1 : Int → MyType
    constructor2 : Float → MyType
;

one : Int = _
pi : Float = _
true : Bool = _
abs : Int → Int = _
add : Int → Int → Int = _
id : ∀a ⇒ a → a = \\t ⇒ \x ⇒ x
bad_id : ∀a ⇒ a → a = \x ⇒ x
float_to_int : Float → (id @ Type Int) = \x ⇒ one
polymorphic : ∀a ⇒ a = _
scoped : ∀a ⇒ (∀s ⇒ s → a) → a = _
f = one
